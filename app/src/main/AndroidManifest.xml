<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.sig.baubau">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <meta-data
        android:name="com.google.android.gms.version"
        android:value="@integer/google_play_services_version" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".UI.TambahMapPosko"></activity>
        <activity android:name=".UI.UbahWilPosko" />
        <activity android:name=".UI.TambahWilPosko" />
        <activity android:name=".UI.WilayahPosko" />
        <activity android:name=".UI.UbahWilLongsor" />
        <activity android:name=".UI.TambahWilLongsor" />
        <activity android:name=".UI.WilayahLongsor" />
        <activity android:name=".UI.UbahWilBanjir" />
        <activity android:name=".UI.TambahWilBanjir" />
        <activity android:name=".UI.WilayahBanjir" />
        <activity android:name=".UI.Posko" />
        <activity android:name=".UI.TampilMAppp" />
        <activity android:name=".UI.LongsorActivity" />
        <activity android:name=".UI.UbahAkun" />
        <activity android:name=".UI.TambahAkun" />
        <activity android:name=".UI.AkunPenggguna" />
        <activity android:name=".UI.MapEditKec" />
        <activity android:name=".UI.MapAddKec" />
        <activity android:name=".UI.UbahKec" />
        <activity android:name=".UI.Kecamatan" />
        <activity android:name=".UI.TambahKecamatan" />
        <activity android:name=".UI.AdminDashboard" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCTZ1tpRAr8nswAogfyb7FEtTEfRkRY1pM" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity android:name=".UI.BanjirActivity" />
        <activity android:name=".UI.Beranda" />
        <activity android:name=".UI.LoginActivity" />
        <activity
            android:name=".UI.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.MainActivity" />
    </application>

</manifest>